# loop over all fights and compile training dataset

def compile_dataset():
    # for each fighter, aggregate features for each fight. list those.
    # this will be input. 
    # per fight, register who won (and method?).
    # this will be ground truth. 
    # each instance (fight + fighters) should probably be listed twice, but swapped: so no model can infer winner based on input position

    pass